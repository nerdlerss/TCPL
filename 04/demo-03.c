#include <stdio.h>
#define MAXLINE 1000

int main(){
    double sum ,atof(char []);
    char line[MAXLINE];
    //int getline(char line,int max);
    sum = 0;
    while (getline(line,MAXLINE) > 0){
        
    }
    return 0;
}